{
  "name": "ai-video-hub",
  "version": "2.0.0",
  "description": "AI视频生成平台 - 商业化版本",
  "main": "index.html",
  "scripts": {
    "dev": "npx serve .",
    "deploy": "vercel --prod",
    "stripe:listen": "stripe listen --forward-to localhost:3000/api/stripe-webhook"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
