# 环境变量配置说明

## 必需的环境变量

在项目根目录创建 `.env.local` 文件，并配置以下变量：

### Stripe 配置

```env
# Stripe 密钥（从 https://dashboard.stripe.com/apikeys 获取）
STRIPE_SECRET_KEY=sk_your_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_your_publishable_key_here

# Stripe Webhook 密钥（从 Stripe Dashboard 的 Webhooks 设置获取）
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe 价格 ID（从 Stripe Dashboard 创建产品后获取）
STRIPE_PRICE_ID_PRO=price_your_pro_price_id_here
STRIPE_PRICE_ID_ENTERPRISE=price_your_enterprise_price_id_here
```

### 网站配置

```env
# 网站域名（用于Stripe回调）
NEXT_PUBLIC_SITE_URL=https://your-domain.com
```

---

## 本地开发配置

1. 复制 `.env.example` 为 `.env.local`：
   ```bash
   cp .env.example .env.local
   ```

2. 填写你的 Stripe 密钥

---

## Stripe 配置步骤

### 1. 创建 Stripe 账户

1. 访问 https://stripe.com
2. 注册账户

### 2. 获取 API 密钥

1. 进入 Dashboard → Developers → API keys
2. 复制密钥填入 `.env.local`

### 3. 创建产品

1. 进入 Dashboard → Product catalog
2. 创建订阅产品
3. 复制 Price ID 填入配置

---

## 安全注意事项

⚠️ **重要**:
- 永远不要将 Secret Key 暴露给前端
- 不要将 `.env.local` 提交到 Git
- 生产环境使用 HTTPS
